

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v6.5.1 -->
<!-- Updated: 2026-06-12T20:27:30.885Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

# Code signatures

## deps
```
app\attachments\security.py ← __future__, app, ipaddress
app\chat_history\extractor.py ← __future__
app\chat_history\har_importer.py ← __future__, app
app\chat_history\notion_sync.py ← __future__, app
app\chat_history\store.py ← __future__, app
app\core\errors.py ← __future__, fastapi
app\core\models.py ← __future__
tests\test_attachment_normalizer.py ← app, unittest
tests\test_attachment_security.py ← unittest, app
tests\test_chat_history_bulk_delete.py ← app, unittest
tests\test_chat_attachment_upload.py ← unittest, fastapi, app
tests\test_chat_history_extractor.py ← app, unittest
tests\test_notion_attachment_upload.py ← app, unittest
tests\test_notion_client_attachment_upload.py ← unittest, app
tests\test_responses_attachment_upload.py ← __future__, unittest, fastapi, app
tests\test_persist_remote_chat.py ← unittest, fastapi, app
app\account_pool.py ← app
app\api\chat.py ← fastapi, app
app\api\models.py ← fastapi, app
app\config.py ← dotenv
app\conversation.py ← app
app\model_registry.py ← app
app\notion_client.py ← app, requests, urllib3
app\schemas.py ← pydantic
app\server.py ← fastapi, slowapi, app
app\stream_parser.py ← app, requests
app\stream_parser_safe.py ← __future__, app, requests
app\summarizer.py ← app, httpx
tests\test_model_registry.py ← app
tests\test_standard_transcript.py ← app
tests\test_stream_parser_safe.py ← app
```

## changes (last 10 commits — 2 hours ago)
```
tests\test_persist_remote_chat.py             ~test_stream_parser_safe_yields_unique_thinking  ~test_stream_parser_safe_suppresses_duplicate_thinking  ~PersistRemoteChatTests
app\account_pool.py                           ~AccountPool
app\api\chat.py                               +_local_probe_response_text  +_handle_lite_request  +_handle_standard_request  ~_handle_lite_request
app\config.py                                 ~parse_allowed_origins  ~load_accounts
app\conversation.py                           ~ConversationManager  ~compress_sliding_window_round  ~compress_round_if_needed  ~build_standard_transcript
app\logger.py                                 +TimestampedFormatter  +__init__  +setup_uvicorn_logging  ~JsonFormatter
app\model_registry.py                         ~get_notion_model  ~is_gemini_model
app\notion_client.py                          ~_resolve_thread_persistence  ~NotionUpstreamError  ~NotionOpusAPI
app\schemas.py                                ~ChatMessage  ~ChatCompletionRequest  ~ChatCompletionResponse  ~ChatMessageResponseChoice
app\server.py                                 ~_valid_bearer_token  ~lifespan  ~generic_exception_handler  ~log_requests_middleware
app\stream_parser.py                          +_looks_like_tool_call_json_fragment  ~_strip_lang_tags  ~_extract_segment_index  ~_extract_value_index
app\stream_parser_safe.py                     ~parse_stream
app\summarizer.py                             ~is_summarizer_configured  ~summarize_turn
tests\test_model_registry.py                  +test_captured_notion_backend_mappings_are_registered  +test_captured_display_names_are_registered  +test_gemini_3_5_flash_no_longer_uses_markdown_chat_route
tests\test_standard_transcript.py             +test_standard_transcript_answers_latest_user_turn
tests\test_stream_parser_safe.py              +DummyResponse  +test_buffered_thinking_is_not_promoted_after_final_content  +fake_parse_stream  +test_buffered_thinking_fallback_remains_for_legacy_answer_segments
.github\copilot-instructions.md               +create_chat_completion  +list_models  +load_accounts  +is_lite_mode
```

## .github

### .github\copilot-instructions.md
```
h2 Auto-generated signatures
h2 SigMap commands
h1 Code signatures
h2 deps
h2 changes (last 10 commits — 3 days ago)
h2 .github
h3 .github\copilot-instructions.md
h2 app
h3 app\notion_client.py
h3 app\schemas.py
h3 app\account_pool.py
h3 app\api\chat.py
h3 app\api\chat_history.py
h3 app\api\models.py
h3 app\api\responses.py
h3 app\chat_history\extractor.py
h3 app\chat_history\har_importer.py
h3 app\chat_history\notion_sync.py
h3 app\chat_history\store.py
h3 app\config.py
h3 app\conversation.py
h3 app\core\errors.py
h3 app\core\models.py
h3 app\logger.py
h3 app\model_registry.py
```

## app

### app\attachments\security.py
```
class AttachmentPolicy
  def from_env() → "AttachmentPolicy"
def normalize_content_type(content_type: str) → str
def validate_attachment_count(count: int, policy: AttachmentPolicy | None) → None
def validate_content_type(content_type: str, policy: AttachmentPolicy | None) → str
def validate_size(size_bytes: int, policy: AttachmentPolicy | None) → None
def is_blocked_ip(ip_text: str) → bool
def validate_remote_url(url: str, policy: AttachmentPolicy | None) → str
def validate_local_path_allowed(policy: AttachmentPolicy | None) → None
```

### app\chat_history\extractor.py
```
def record_value(record: Any) → dict[str, Any]
def record_maps(obj: Any)
def visible_message_text(value: dict[str, Any]) → str  # Return only the text Notion shows as chat content, excluding
def visible_message_role(value: dict[str, Any]) → str | None
def extract_message_ids(value: dict[str, Any]) → list[str]  # Collect nested Notion thread-message IDs without treating co
def collect_hydration_message_ids(value: Any, depth: int) → list[str]  # Collect nested Notion thread-message IDs without treating co
def normalize_thread(thread_id: str | None, raw: dict[str, Any]) → dict[str, Any] | None
def normalize_message(message_id: str | None, raw: dict[str, Any], fallback_thread_id: str | None) → dict[str, Any] | None
def merge_records_into_bundle(bundle: dict[str, Any], obj: Any) → None
def extract_chat_bundle(obj: Any) → dict[str, Any]
def redact_secrets(value: Any, depth: int) → Any
def describe_thread_record(thread: dict[str, Any] | None, messages: list[dict[str, Any]] | None) → dict[str, Any]
```

### app\chat_history\har_importer.py
```
def import_chat_object(obj: Any) → dict[str, Any]
def import_har_object(har: dict[str, Any]) → dict[str, Any]
```

### app\chat_history\notion_sync.py
```
def hydrate_message_ids_from_notion(client: NotionOpusAPI, message_ids: list[str] | set[str], *, fallback_thread_id: str | None, hydrate_batch_size: int) → dict[str, Any]  # Hydrate specific Notion thread-message IDs into a chat-histo
def hydrate_thread_record_from_notion(client: NotionOpusAPI, thread_id: str) → dict[str, Any]
def hydrate_thread_from_notion(client: NotionOpusAPI, thread: dict[str, Any], *, hydrate_batch_size: int) → dict[str, Any]  # Hydrate only the messages referenced by one selected archive
def sync_chat_history_from_notion(client: NotionOpusAPI, *, limit: int, max_pages: int, hydrate: bool, hydrate_batch_size: int) → dict[str, Any]  # Read-only direct sync from Notion transcript RPCs into the l
```

### app\chat_history\store.py
```
class ChatHistoryStore
  def __init__(db_path: str | None)
  def upsert_bundle(bundle: dict[str, Any]) → dict[str, int]
  def existing_thread_ids(thread_ids: list[Any]) → set[str]
  def delete_threads(thread_ids: list[Any]) → dict[str, int]
  def list_threads(limit: int, offset: int, include_inactive: bool) → list[dict[str, Any]]
  def get_thread(thread_id: str) → dict[str, Any] | None
  def debug_thread(thread_id: str) → dict[str, Any]
  def search(query: str, limit: int) → list[dict[str, Any]]
def get_default_chat_history_db_path() → str
```

### app\core\errors.py
```
def openai_error_payload(message: str, code: str, status_code: int, error_type: str, param: str | None) → dict[str, Any]
def openai_error(message: str, code: str, status_code: int, error_type: str, param: str | None) → NoReturn
def openai_error_response(message: str, code: str, status_code: int, error_type: str, param: str | None) → JSONResponse
```

### app\core\models.py
```
def normalize_model_id(model: str | None) → str | None
```

### app\account_pool.py
```
class AccountPool
  def __init__(accounts: List[dict])
  def get_client(wait_if_cooling: bool) → NotionOpusAPI
  def get_status_summary() → Dict[str, int]
  def mark_failed(client: NotionOpusAPI, cooldown_seconds: int)
```

### app\api\chat.py
```
async def create_chat_completion(request: Request, req_body: ChatCompletionRequest, background_tasks: BackgroundTasks, response: Response)  # text OpenAI APItext
async def delete_conversation(conversation_id: str, request: Request)  # Delete a conversation by its ID
POST /chat/completions  →  create_chat_completion()
DELETE /conversations/{conversation_id}  →  delete_conversation()
```

### app\api\models.py
```
async def list_models() → Dict[str, Any]  # List available models in OpenAI-compatible format
GET /models  →  list_models()
```

### app\config.py
```
def parse_allowed_origins(value: str | None, *, allow_unsafe_wildcard: bool) → list[str]  # Parse and harden CORS origins
def load_accounts()  # text accounts
def is_lite_mode() → bool  # Standard text thinking text
def is_standard_mode() → bool  # Standard text thinking text
def get_default_account()  # text
```

### app\conversation.py
```
class ConversationManager
  WINDOW_SIZE=16
  WINDOW_ROUNDS=8
  SUMMARY_INJECT_LIMIT=15
  def __init__()
  def new_conversation() → str
  def get_conversation_thread_id(conversation_id: str) → str|None
  def get_conversation_thread_model(conversation_id: str) → str|None
  def clear_conversation_thread(conversation_id: str) → None
  def conversation_exists(conversation_id: str) → bool
  def add_message(conversation_id: str, role: str, content: str, thinking: str) → None
  def migrate_all_conversations() → Dict[str, int]
async def compress_sliding_window_round(manager: ConversationManager, conversation_id: str, round_number: int) → bool  # text
async def compress_round_if_needed(manager: ConversationManager, conversation_id: str) → None  # Move old turns out of sliding window, archive raw text, and 
def build_lite_transcript(user_prompt: str, model_name: str) → list[dict[str, Any]]  # text Lite text transcripttext config + usertext
def build_standard_transcript(messages: list[dict[str, Any]], model_name: str, account: dict) → list[dict[str, Any]]  # text Standard text transcripttext
```

### app\logger.py
```
class JsonFormatter(logging.Formatter)
  def format(record)
class TimestampedFormatter(logging.Formatter)
  def __init__()
def setup_uvicorn_logging() → None  # Patch uvicorn's access and error loggers to emit timestamps
def setup_logger(name)  # Configure and return the global singleton logger
```

### app\model_registry.py
```
def get_notion_model(model_name: str) → str  # Return whether the model belongs to the Gemini family
def is_gemini_model(model_name: str) → bool  # Return whether the model belongs to the Gemini family
def get_thread_type(model_name: str) → str  # Resolve the Notion thread type for a model
def get_standard_model(model_name: str) → str
def list_available_models() → list[str]
def is_supported_model(model_name: str) → bool
def get_display_name(model_name: str) → str
def get_model_icon(model_name: str) → str
```

### app\notion_client.py
```
class NotionUpstreamError(RuntimeError)
class NotionOpusAPI
  def __init__(account_config: dict)
  def request_upload_descriptor(*, name: str, content_type: str, size: int, thread_id: str | None, create_thread: bool) → dict[str, Any]
  def perform_multipart_upload(*, descriptor: dict[str, Any], name: str, data: bytes, content_type: str) → None
  def enqueue_attachment_processing(*, attachment_url: str, thread_id: str) → str
  def get_task_status(task_id: str) → dict[str, Any]
  def get_signed_read_url(attachment_url: str, thread_id: str, download_name: str) → str
  def fetch_chat_history(limit: int, max_pages: int) → dict[str, Any]
  def delete_threads(thread_ids: list[str]) → dict[str, Any]
```

### app\schemas.py
```
class ChatMessage(BaseModel) {role*, content*}
class ChatCompletionRequest(BaseModel) {messages*}
class ChatMessageResponseChoice(BaseModel) {message*}
class ChatCompletionResponse(BaseModel) {id*, model*, choices*}
class ChatCompletionChunkDelta(BaseModel)
class ChatCompletionChunkChoice(BaseModel) {delta*}
class ChatCompletionChunk(BaseModel) {id*, model*, choices*}
```

### app\server.py
```
async def lifespan(app: FastAPI)
def custom_rate_limit_exceeded_handler(request: Request, exc: RateLimitExceeded)
async def http_exception_handler(request: Request, exc: HTTPException)
async def generic_exception_handler(request: Request, exc: Exception)
async def log_requests_middleware(request: Request, call_next)
async def api_key_auth(request: Request, call_next)
async def favicon()
def health_check(request: Request)
def healthz(request: Request)
def chat_history_import_js()
def chat_history_browser_js()
def chat_history_main_js()
def chat_history_resume_js()
def attachment_settings_js()
def frontend_index(request: Request)
GET /favicon.ico  →  favicon()
GET /health  →  health_check()
GET /healthz  →  healthz()
GET /chat-history-import.js  →  chat_history_import_js()
GET /chat-history-browser.js  →  chat_history_browser_js()
GET /chat-history-main.js  →  chat_history_main_js()
GET /chat-history-resume.js  →  chat_history_resume_js()
GET /attachment-settings.js  →  attachment_settings_js()
GET /  →  frontend_index()
```

### app\stream_parser.py
```
def parse_stream(response: requests.Response) → Generator[dict[str, Any], N...  # text Notion NDJSON text
```

### app\stream_parser_safe.py
```
def parse_stream(response: requests.Response) → Generator[dict[str, Any], N...  # Yield content/search/final events while suppressing streamed
```

### app\summarizer.py
```
class SummarizerUnavailableError(Exception)
def is_summarizer_configured() → bool
async def summarize_turn(old_summaries: list[str], user_msg: str, assistant_msg: str) → str  # text LLM text
```

## frontend

### frontend\index.html
```
title: Notion AI Studio
div#mobileBackdrop
aside#sidebar
div#sidebarResizeHandle
input#searchInput
button#newChatBtn
div#chatList
button#settingsBtn
button#openSidebarBtn
span#headerTitle
span#tokenCount
button#themeToggleBtn
svg#sunIcon
svg#moonIcon
div#memoryBanner
button#memoryBannerClose
div#chatContainer
div#welcomeScreen
h1#welcomeGreeting
div#welcomeSuggestions
div#inputAreaWrapper
textarea#chatInput
div#attachmentBar
input#attachmentInput
div#modelSelectorContainer
```

## tests

### tests\test_attachment_normalizer.py
```
class AttachmentNormalizerTests(unittest.TestCase)
  def test_top_level_attachment_adds_fallback_prompt() → None
  def test_content_array_extracts_text_and_file_data() → None
  def test_image_url_extracts_remote_url() → None
  def test_responses_input_preserves_attachment_parts() → None
  def test_local_windows_path_is_detected() → None
```

### tests\test_attachment_security.py
```
class AttachmentSecurityTests(unittest.TestCase)
  def test_rejects_unsupported_content_type() → None
  def test_rejects_oversized_payload() → None
  def test_blocks_localhost_remote_url() → None
  def test_blocks_private_resolved_address(getaddrinfo) → None
  def test_allows_public_resolved_address(getaddrinfo) → None
  def test_decode_data_url() → None
  def test_load_inline_attachment() → None
  def test_local_path_disabled_by_default() → None
```

### tests\test_chat_history_bulk_delete.py
```
class ChatHistoryBulkDeleteTests(unittest.TestCase)
  def test_existing_thread_ids_filters_unknown_and_non_scalar_ids() → None
  def test_delete_threads_removes_threads_messages_and_fts_rows() → None
  def test_bulk_delete_ui_consumes_per_thread_results() → None
```

### tests\test_chat_attachment_upload.py
```
class ChatAttachmentRouteTests(unittest.TestCase)
  def setUp()
  def tearDown()
  def test_no_attachments_preserves_behavior()
  def test_attachments_disabled_returns_400()
  def test_enabled_invalid_base64_returns_400()
  def test_content_array_file_normalized_and_stream_calls_uploader()
```

### tests\test_chat_history_extractor.py
```
class ChatHistoryExtractorTests(unittest.TestCase)
  def test_extract_message_id_field_variants() → None
  def test_normalize_message_field_variants() → None
  def test_merge_records_with_inline_conversation_messages() → None
  def test_describe_thread_record_includes_message_raw_fields() → None
  def test_normalize_message_uses_nested_data_title_as_text_fallback() → None
  def test_normalize_thread_preserves_numeric_notion_timestamps() → None
  def test_agent_inference_uses_visible_text_only() → None
  def test_tool_result_records_are_not_visible_messages() → None
```

### tests\test_notion_attachment_upload.py
```
class FakeNotionClient
  def __init__()
  def request_upload_descriptor(name, content_type, size, thread_id, create_thread)
  def perform_multipart_upload(descriptor, name, data, content_type)
  def enqueue_attachment_processing(attachment_url, thread_id)
  def get_task_status(task_id)
  def get_signed_read_url(attachment_url, thread_id, download_name)
class NotionAttachmentUploadTests(unittest.TestCase)
  def test_upload_attachments_success()
  def test_task_failure_raises()
  def failing_status(task_id)
  def test_missing_client_method_fails()
  def test_incomplete_descriptor_fails()
  def request_upload_descriptor(name, content_type, size, thread_id, create_thread)
  def test_multipart_upload_failure_propagates()
  def perform_multipart_upload(descriptor, name, data, content_type)
```

### tests\test_notion_client_attachment_upload.py
```
class NotionClientAttachmentTests(unittest.TestCase)
  def setUp()
  def test_request_upload_descriptor_payload_and_response()
  def test_request_upload_descriptor_nested_file_id_and_aliases()
  def test_request_upload_descriptor_missing_required_fields_fails()
  def test_request_upload_descriptor_http_error()
  def test_perform_multipart_upload_uses_requests()
  def test_enqueue_task_and_get_status()
  def test_get_signed_read_url()
```

### tests\test_responses_attachment_upload.py
```
class ResponsesAttachmentTests(unittest.TestCase)
  def setUp()
  def tearDown()
  def test_no_attachments_preserves_behavior()
  def test_input_file_normalized_and_passed()
  def test_top_level_attachments_supported()
  def test_response_route_forwards_attachments_to_notion_stream()
  def test_attachments_disabled_returns_400()
  def test_enabled_invalid_base64_returns_400()
```

### tests\test_persist_remote_chat.py
```
class PersistRemoteChatTests(unittest.TestCase)
  def setUp()
  def tearDown()
  def test_schema_accepts_metadata()
  def test_stream_response_persistence_override_true()
  def mock_parse_stream(res)
  def test_stream_response_persistence_override_false()
  def mock_parse_stream(res)
  def test_route_forwards_persist_remote_chat()
```

### tests\test_model_registry.py
```
def test_captured_notion_backend_mappings_are_registered()
def test_captured_display_names_are_registered()
def test_gemini_3_5_flash_no_longer_uses_markdown_chat_route()
```

### tests\test_standard_transcript.py
```
def test_standard_transcript_answers_latest_user_turn()
```

### tests\test_stream_parser_safe.py
```
class DummyResponse
def test_buffered_thinking_is_not_promoted_after_final_content(monkeypatch)
def test_buffered_thinking_fallback_remains_for_legacy_answer_segments(monkeypatch)
```
